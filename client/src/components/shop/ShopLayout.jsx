import React from "react";
import { Outlet } from "react-router-dom";

const ShopLayout = () => {
  return (
    <div>
      ShopLayout
      <Outlet />
    </div>
  );
};

export default ShopLayout;
